/**
 * Tipos de ambiente del proyecto.
 *
 * El sitio es 100% estático y no usa variables de entorno todavía, así que
 * acá solo se referencian los tipos que genera Astro. Cuando se conecte el
 * backend, las variables van declaradas en `ImportMetaEnv` para que
 * `import.meta.env.LO_QUE_SEA` quede tipado y un typo falle en compilación.
 */
